{
    "name": "voight",
    "private": true,
    "type": "module",
    "module": "index.ts",
    "scripts": {
        "fmt": "oxfmt",
        "fmt:check": "oxfmt --check",
        "test": "vitest run",
        "test:watch": "vitest",
        "ts:check": "tsc --noEmit"
    },
    "devDependencies": {
        "oxfmt": "^0.42.0",
        "vitest": "^4.1.2"
    },
    "peerDependencies": {
        "typescript": "^6"
    }
}
